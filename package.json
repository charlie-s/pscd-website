{
  "scripts": {
    "start": "eleventy --serve & npx tailwindcss -i src/css/style.css -c tailwind.config.js -o dist/css/style.css --watch",
    "build": "ELEVENTY_PRODUCTION=true eleventy && NODE_ENV=production npx tailwindcss -i src/css/style.css -c tailwind.config.js -o dist/css/style.css --minify"
  },
  "dependencies": {
    "@11ty/eleventy": "^1.0.1",
    "autoprefixer": "^10.4.2",
    "cssnano": "^5.1.0",
    "postcss": "^8.4.7",
    "postcss-import": "^14.0.2",
    "tailwindcss": "^3.0.23"
  }
}
